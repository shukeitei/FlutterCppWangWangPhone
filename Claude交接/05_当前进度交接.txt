# 当前进度交接（2026年4月11日更新）

## 上次做到哪里
ST vs 汪汪机 payload 大盘点完成。采样参数 + 角色卡高级定义 + 详情页展示面板 + assistant prefill 全部落地。
单聊完全正常，群聊有消息格式合规性问题（连续同 role 消息导致 API 400），需要专项修复。
main 分支待合并。

## 下一步第一件事
**群聊消息格式专项治理**——多角色回复产生连续 assistant 消息，与 DeepSeek API 严格交替要求冲突。
这是根本性架构问题，不是简单合并能解决的，需要重新设计群聊的 messages 构建策略。

## 紧接着要做的
1. 群聊消息格式专项治理（最高优先级）
2. chatHistory 分水岭——预设中 chatHistory marker 后面的指令块应放到 user message 而非 system
3. prefill 对 deepseek-reasoner 的支持——目前 reasoner 跳过了 prefill（API 返回 400）
4. 上下文调试页打通
5. 引用功能

## 本次大盘点发现的关键差异（ST vs 汪汪机）

### 已修复的差异
| 差异 | 状态 |
|------|------|
| 采样参数缺失（temperature/top_p/penalties） | ✅ 已修复 + UI 面板 |
| 角色卡高级定义缺失（mes_example/depth_prompt/personality 等） | ✅ 已修复 |
| 桥接不读 v2/v3 卡的 data.* 字段 | ✅ 已修复 |
| 桥接不读 iTXt chunk | ✅ 已修复 |
| assistant prefill 塞在 system prompt 末尾 | ✅ 已拆分为独立消息（reasoner 跳过） |
| max_tokens / history_count 硬编码 | ✅ 已做成可配置 + UI |

### 未修复的差异
| 差异 | 说明 |
|------|------|
| chatHistory 分水岭 | ST 的 chatHistory marker 之后的预设指令放在 user message 里；汪汪机全放 system |
| prefill 对 reasoner | deepseek-reasoner 不接受 prefix:true 的 assistant 消息（返回 400），目前跳过 |
| 群聊连续同 role | 群聊多角色回复产生连续 assistant 消息，_mergeConsecutiveRoles 合并后仍有边界情况导致 400 |

## 采样参数系统
- 存储：SharedPreferences，keys 以 `ai_` 前缀
- getter/setter 在 chat_controller.dart（搜 `_temperature`）
- UI：chat_app_page.dart 的 `_AiParamsCard` + `_AiParamsSheet`
- 位置：微信"我"页面，API 接口卡片下方
- 默认值参考 ST：temperature=1.21, top_p=0.96, presence_penalty=0.22, frequency_penalty=0.56
- `resetAiParams()` 一键恢复默认

## 角色卡高级定义
- 桥接 extract_character() 已支持 tEXt + iTXt chunk
- load_all_characters() 优先读 data.*（v2/v3），顶层做 fallback（v1）
- 新增返回字段：mes_example, system_prompt, post_history_instructions, depth_prompt{prompt,depth,role}, alternate_greetings
- assembleSystemPrompt 返回类型改为 Record：(system, assistantPrefill, postHistory, depthPromptText, depthPromptDepth, depthPromptRole)
- dialogueExamples marker 现在映射到 mesExample
- charSystemPrompt 放在 parts 开头
- assistant role 的预设词条自动提取为 prefill，不混入 system prompt
- depth_prompt 按 depth 值插入 messages 倒数第 N 条
- post_history_instructions 追加到 messages 末尾

## _mergeConsecutiveRoles 说明
- 静态方法，对 messages 数组做原地修改
- 第一轮：非首条 system 转为 user
- 第二轮：合并连续同 role 消息
- 单聊工作正常；群聊因多角色回复 + depth_prompt 插入的交互复杂，第二次触发仍会 400
- **群聊需要专项重构，不是简单合并能解决的**

## 角色详情页高级设定面板
- character_detail_page.dart 新增 `_AdvancedSettingsCard`
- 只读展示，点击展开从桥接拉数据
- 显示：personality / mes_example / system_prompt / post_history_instructions / depth_prompt
- 每个字段可折叠，显示字符数 badge
- 空字段显示"未设置"

## Dump 调试机制（临时）
- chat_controller.dart 顶部有 `_dumpRequestPayload()` 方法
- 单聊 dump 写到 `/sdcard/Download/wangwang_dump_single.json`
- 群聊 dump 写到 `/sdcard/Download/wangwang_dump_group.json`
- `adb pull /storage/emulated/0/Download/wangwang_dump_single.json` 拉到本地分析
- AndroidManifest.xml 已加 WRITE_EXTERNAL_STORAGE / MANAGE_EXTERNAL_STORAGE 权限
- **上线前记得删除 dump 逻辑和存储权限**

## 改动文件清单（本次）
- wangwang_bridge.py — extract_character 加 iTXt + load_all_characters 加 data.* 优先 + 新字段
- chat_controller.dart — 采样参数存储/注入 + assembleSystemPrompt 大重构 + prefill 提取 + _mergeConsecutiveRoles + _dumpRequestPayload
- chat_app_page.dart — _AiParamsCard + _AiParamsSheet UI
- character_detail_page.dart — _AdvancedSettingsCard 高级设定面板
- AndroidManifest.xml — 存储权限（临时，为 dump 调试用）

## 持久化文件清单（更新）
- wangwang_messages.json — 聊天记录
- wangwang_groups.json — 群聊列表
- wangwang_preset_overrides.json — 预设选择+词条开关覆盖
- wangwang_world_bindings.json — 世界书绑定
- shared_preferences — 全局 persona、API 配置、**AI 采样参数（新增）**
- ~/Developer/persona_bindings.json — 单聊 persona 绑定

## 给下一个 Claude 的提示
- assembleSystemPrompt 返回值现在是 Record 不是 String，所有调用方都已改
- prefill 对 reasoner 跳过（`!_apiModelId.contains('reasoner')`），如果用户换了非 reasoner 模型，prefill 会自动生效
- 群聊的核心难题：多角色回复天然产生连续 assistant 消息，而 DeepSeek API 要求严格交替。当前的 _mergeConsecutiveRoles 在第二轮触发时仍会出问题。需要从 _buildGroupHistory 源头重新设计
- dump 文件还在，可以 `adb pull` 用来调试。上线前要删
- 桥接改了 extract_character，重启 ST 或手动重启桥接才生效
